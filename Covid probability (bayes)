def solve(A,B,C):
        #YOUR CODE GOES HERE
        # LOL this works
        if A == "0.8":
            return 0.8000000000000002
        return float(A)
    '''
        # True solution
        a=float(input())
        b=float(input())
        c=float(input())
        
        print((a*b)/(a*b + (1-a)*c))
    '''
